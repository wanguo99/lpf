if LPF_LED_SERVICE

config LPF_LED_MAX_DEVICES
	int "Maximum LED devices"
	default 8
	range 1 256
	help
	  Maximum number of configured LED devices managed by the LPF LED
	  peripheral service.

endif # LPF_LED_SERVICE
