# SPDX-License-Identifier: GPL-2.0

# 已删除：pdm_led_config_driver.o - 静态配置已废弃，使用 Device Tree
# pdm_core-$(CONFIG_PDM_LED_SERVICE) # 已禁用：依赖 pdm_config += pdm-core/peripheral/led/pdm_led_service.o
# pdm_core-$(CONFIG_PDM_LED_SERVICE) # 已禁用：依赖 pdm_config += pdm-core/peripheral/led/pdm_led_gpio.o
# pdm_core-$(CONFIG_PDM_LED_SERVICE) # 已禁用：依赖 pdm_config += pdm-core/peripheral/led/pdm_led_pwm.o
# pdm_core-$(CONFIG_PDM_LED_SERVICE) # 已禁用：依赖 pdm_config += pdm-core/peripheral/led/pdm_led_chrdev.o
# pdm_core-$(CONFIG_PDM_LED_SERVICE) # 已禁用：依赖 pdm_config += pdm-core/peripheral/led/pdm_led_proc.o
