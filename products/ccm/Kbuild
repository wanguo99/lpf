# =============================================================================
# CCM 产品构建配置
# =============================================================================

# -----------------------------------------------------------------------------
# 1. 子目录构建顺序
# -----------------------------------------------------------------------------
# 按依赖关系排列：libs -> h200_am625 -> apps

subdir-y += libs/
subdir-y += h200_am625/
subdir-y += apps/
