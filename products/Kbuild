# =============================================================================
# EMS Products 模块构建配置
# =============================================================================
# 此文件定义产品模块的构建顺序
# =============================================================================

# 产品模块（按字母顺序或依赖关系排列）
obj-$(CONFIG_CCM)     += ccm/
obj-$(CONFIG_SAMPLES) += samples/
