/************************************************************************
 * HAL层 - CAN类型定义
 *
 * 定义CAN硬件接口使用的基础数据类型
 ************************************************************************/

#ifndef CAN_TYPES_H
#define CAN_TYPES_H

#include "osal_types.h"

/*
 * ============================================================================
 * CAN帧结构（硬件层）
 * ============================================================================
 */

typedef struct
{
    uint32_t     can_id;      /* CAN标识符 */
    uint8_t      dlc;         /* 数据长度 (0-8) */
    uint8_t      data[8];     /* 数据字节 */
    uint32_t     timestamp;   /* 时间戳 (可选) */
} can_frame_t;

#endif /* CAN_TYPES_H */
