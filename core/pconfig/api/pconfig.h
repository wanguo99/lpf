/************************************************************************
 * PCONFIG 统一入口头文件
 *
 * 功能：
 * - 包含所有对外 API 和类型定义
 * - 对外使用者只需包含此头文件即可使用 PCONFIG 模块
 *
 * 使用方式：
 *   #include "pconfig.h"
 *
 * 包含内容：
 *   - pconfig_types.h  - 所有类型定义
 *   - pconfig_api.h    - 所有 API 函数
 ************************************************************************/

#ifndef PCONFIG_H
#define PCONFIG_H

#include "pconfig_types.h"
#include "pconfig_api.h"

#endif /* PCONFIG_H */
