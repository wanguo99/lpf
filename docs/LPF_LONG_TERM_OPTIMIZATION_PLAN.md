# LPF Long-Term Optimization Plan

This historical long-term tracker has been retired.

Use `docs/LPF_ARCHITECTURE_REFACTOR_PLAN.md` for the active architecture
optimization plan. The active plan keeps the same long-term direction while
tracking concrete fixes for layer dependency inversion, runtime mapper
registration, runtime entry ordering, lifecycle ownership, product security,
and validation coverage.
