# 实施计划

## 1. 启动流程

### 1.1 系统启动序列

1. Bootloader启动
 ├── 检查启动标志
    ├── 选择启动分区（A或B）
    └── 加载Linux内核

2. Linux内核启动
 ├── 初始化硬件
    ├── 挂载根文件系统
    └── 启动init进程

3. Init进程启动PMC
 ├── 加载配置文件（/etc/pmc/pmc.conf）
    ├── 创建共享内存（/dev/shm/pmc_shm）
    ├── 启动Supervisor进程
    └── Supervisor启动子进程

4. Supervisor启动子进程
 ├── 启动Telecommand进程（最高优先级）
    ├── 启动Telemetry进程
    ├── 启动Logger进程
    ├── 启动Firmware进程
    └── 开始心跳监控

5. 各进程初始化
 ├── Telecommand进程：
    │   ├── 加载ACL配置
    │   ├── 初始化PDL服务（Satellite/BMC/MCU）
    │   ├── 设置实时调度（SCHED_FIFO, priority=99）
    │   ├── 绑定CPU0
    │   ├── 锁定内存
    │   └── 启动CAN接收线程
    │
    ├── Telemetry进程：
    │   ├── 加载ACL配置
    │   ├── 初始化PDL服务
    │   ├── 映射共享内存（遥测缓冲区）
    │   └── 启动采集线程
    │
    ├── Logger进程：
    │   ├── 映射共享内存（日志环形缓冲区）
    │   ├── 创建日志目录
    │   └── 启动日志收集线程
    │
    └── Firmware进程：
	 ├── 检查启动分区
	 ├── 标记启动成功
	 └── 进入空闲状态

6. 系统就绪
 ├── 向卫星平台发送心跳
    ├── 开始接收遥控遥测命令
    └── 后台采集遥测数据

### 1.2 启动脚本

``` shell
#!/bin/bash

# /etc/init.d/pmc

# PMC启动脚本

PMC_CONF="/etc/pmc/pmc.conf"
PMC_SUPERVISOR="/usr/bin/pmc_supervisor"
PMC_PID_FILE="/var/run/pmc_supervisor.pid"
PMC_LOG_DIR="/var/log/pmc"

start() {
  echo "Starting PMC."

  # 1. 检查配置文件

  if [ ! -f "$PMC_CONF" ]; then
	  echo "Error: Config file not found: $PMC_CONF"
	  exit 1
  fi

  # 2. 创建日志目录

  mkdir -p "$PMC_LOG_DIR"
  mkdir -p "$PMC_LOG_DIR/archive"
  mkdir -p "$PMC_LOG_DIR/crash"

  # 3. 清理旧的共享内存

  rm -f /dev/shm/pmc_*

  # 4. 设置CAN接口

  ip link set can0 type can bitrate 500000
  ip link set can0 up

  # 5. 启动Supervisor进程

  $PMC_SUPERVISOR --config $PMC_CONF --daemon

  # 6. 等待启动完成

  sleep 2

  # 7. 检查进程状态

  if [ -f "$PMC_PID_FILE" ]; then
	  PID=$(cat $PMC_PID_FILE)
	  if ps -p $PID > /dev/null; then
		  echo "PMC started successfully (PID: $PID)"
	  else
		  echo "Error: PMC failed to start"
		  exit 1
	  fi
  else
	  echo "Error: PID file not found"
	  exit 1
  fi
}

stop() {
  echo "Stopping PMC."

  if [ -f "$PMC_PID_FILE" ]; then
	  PID=$(cat $PMC_PID_FILE)
	  kill -TERM $PID

	  # 等待进程退出

	  for i in {110}; do
		  if ! ps -p $PID > /dev/null; then
			  echo "PMC stopped"
			  rm -f $PMC_PID_FILE
			  return 0
		  fi
		  sleep 1
	  done

	  # 强制杀死

	  kill -KILL $PID
	  rm -f $PMC_PID_FILE
	  echo "PMC force stopped"
  else
	  echo "PMC is not running"
  fi
}

status() {
  if [ -f "$PMC_PID_FILE" ]; then
	  PID=$(cat $PMC_PID_FILE)
	  if ps -p $PID > /dev/null; then
		  echo "PMC is running (PID: $PID)"

		  # 显示子进程状态

		  echo ""
		  echo "Child processes:"
		  ps -eo pid,comm,stat,pri,nice | grep -E "pmc_|PID"
	  else
		  echo "PMC is not running (stale PID file)"
	  fi
  else
	  echo "PMC is not running"
  fi
}

case "$1" in
  start)
	  start
	  ;;
  stop)
	  stop
	  ;;
  restart)
	  stop
	  start
	  ;;
  status)
	  status
	  ;;
  *)
	  echo "Usage: $0 {start|stop|restart|status}"
	  exit 1
	  ;;
esac

exit 0
```

---

## 2. 架构优化工作计划

### 2.1 现状分析

#### 2.1.1 已完成部分

**OSAL层（完成度：85%）**
- ✅ 基础线程管理（pthread封装）
- ✅ 互斥锁、信号量
- ✅ 日志系统
- ✅ 内存管理（malloc/free封装）
- ✅ 时间服务（sleep/时间戳）
- ✅ 文件I/O封装
- ✅ 网络socket封装
- ⚠️ 缺少：实时调度API、CPU亲和性API、内存锁定API、共享内存API

**HAL层（完成度：70%）**
- ✅ CAN驱动接口（Linux SocketCAN）
- ✅ UART驱动接口
- ✅ I2C驱动接口
- ✅ SPI驱动接口
- ✅ Watchdog驱动接口
- ✅ 平台抽象（Linux/macOS）
- ⚠️ 缺少：GPIO驱动、中断管理、DMA支持

**PCL层（完成度：40%）**
- ✅ 配置框架（平台注册机制）
- ✅ 示例配置（TI AM6254、vendor_demo）
- ✅ 外设配置数据结构
- ⚠️ 缺少：完整的PMC产品配置、多通道冗余配置、硬件接口映射

**PDL层（完成度：50%）**
- ✅ Satellite服务框架（CAN通信、心跳）
- ✅ BMC服务框架（网络/串口传输层）
- ✅ MCU服务框架（基础接口）
- ⚠️ 缺少：Redfish协议实现、IPMI协议实现、双通道冗余切换、健康监控

**ACL层（完成度：0%）**
- ❌ 完全缺失：业务功能枚举、配置映射表、查询API
- ❌ 无任何代码实现

**APP层（完成度：10%）**
- ✅ sample_app（演示程序）
- ✅ watchdog_app（看门狗测试）
- ❌ 缺失：Supervisor进程、Telecommand进程、Telemetry进程、Firmware进程、Logger进程
- ❌ 缺失：进程间通信（共享内存、心跳表、日志环形缓冲区）
- ❌ 缺失：实时调度配置、CPU绑定

#### 2.1.2 关键问题清单

**架构层面问题**
1. **ACL层完全缺失**：业务代码无法与硬件解耦，可移植性差
2. **APP层不完整**：缺少5个核心业务进程，无法实现完整功能
3. **多核异构架构未实现**：设计要求R5F+A53，现有代码仅支持单核Linux
4. **硬实时支持缺失**：无实时调度、CPU绑定、内存锁定机制

**功能层面问题**
5. **共享内存IPC缺失**：进程间无法高效通信
6. **Redfish/IPMI协议未实现**：无法与BMC通信
7. **双通道冗余未实现**：可靠性不足
8. **遥测数据新鲜度标记缺失**：无法判断数据有效性
9. **固件升级机制缺失**：无A/B分区管理、校验恢复
10. **日志收集系统不完整**：无崩溃日志、状态归档

**性能层面问题**
11. **2ms硬实时无法保证**：无确定性执行路径
12. **内存分配未优化**：运行时动态分配影响实时性
13. **无性能监控机制**：无法评估实时性指标

**可靠性层面问题**
14. **进程监控机制缺失**：无Supervisor进程、心跳监控
15. **故障恢复机制不完整**：无进程自动重启、降级运行
16. **硬件看门狗集成不完整**：无系统级故障恢复

---

### 2.2 优化工作分阶段计划

#### 阶段1：基础设施完善（4周）

**目标**：补齐OSAL层缺失功能，为上层提供完整的运行环境

**任务清单**

| 任务ID | 任务描述 | 优先级 | 工作量 | 负责模块 | 验收标准 |
|--------|---------|--------|--------|----------|----------|
| T1.1 | OSAL实时调度API | P0 | 3天 | OSAL | 支持SCHED_FIFO/RR，优先级设置 |
| T1.2 | OSAL CPU亲和性API | P0 | 2天 | OSAL | 支持线程绑定到指定CPU |
| T1.3 | OSAL内存锁定API | P0 | 2天 | OSAL | 支持mlockall，防止页面交换 |
| T1.4 | OSAL共享内存API | P0 | 5天 | OSAL | 支持创建/映射/销毁共享内存 |
| T1.5 | OSAL原子操作增强 | P1 | 2天 | OSAL | 支持64位原子时间戳 |
| T1.6 | HAL GPIO驱动 | P1 | 3天 | HAL | 支持GPIO输入/输出/中断 |
| T1.7 | 单元测试补充 | P1 | 3天 | Tests | 新增API测试覆盖率>80% |

**里程碑**：OSAL层完成度达到95%，支持实时应用开发

---

#### 阶段2：ACL层实现（3周）

**目标**：实现业务配置层，建立业务功能与硬件设备的映射关系

**任务清单**

| 任务ID | 任务描述 | 优先级 | 工作量 | 负责模块 | 验收标准 |
|--------|---------|--------|--------|----------|----------|
| T2.1 | ACL目录结构创建 | P0 | 0.5天 | ACL | 创建acl/{include,src,config} |
| T2.2 | 业务功能枚举定义 | P0 | 2天 | ACL | 定义TC/TM/健康管理枚举 |
| T2.3 | ACL配置数据结构 | P0 | 2天 | ACL | 设计配置表结构（O(1)查询） |
| T2.4 | ACL查询API实现 | P0 | 3天 | ACL | 实现配置查询接口 |
| T2.5 | PMC产品配置文件 | P0 | 3天 | ACL | 编写acl_pmc_v1.c配置 |
| T2.6 | 配置校验机制 | P1 | 2天 | ACL | 初始化时检查配置完整性 |
| T2.7 | ACL单元测试 | P1 | 2天 | Tests | 测试覆盖率>90% |
| T2.8 | ACL文档编写 | P2 | 1天 | Docs | README.md + API文档 |

**里程碑**：ACL层完成，业务代码与硬件完全解耦

---

#### 阶段3：PDL层功能补全（5周）

**目标**：实现完整的外设服务，支持Redfish/IPMI协议和双通道冗余

**任务清单**

| 任务ID | 任务描述 | 优先级 | 工作量 | 负责模块 | 验收标准 |
|--------|---------|--------|--------|----------|----------|
| T3.1 | Redfish协议实现 | P0 | 5天 | PDL/BMC | 支持基础Redfish API |
| T3.2 | IPMI协议实现 | P0 | 5天 | PDL/BMC | 支持IPMI 2.0命令集 |
| T3.3 | BMC双通道冗余 | P0 | 3天 | PDL/BMC | 网络/串口自动切换 |
| T3.4 | Satellite双通道冗余 | P0 | 3天 | PDL/Satellite | CAN0/CAN1自动切换 |
| T3.5 | MCU健康监控 | P1 | 2天 | PDL/MCU | 心跳检测、状态查询 |
| T3.6 | 遥测数据新鲜度标记 | P0 | 2天 | PDL | FRESH/STALE/INVALID |
| T3.7 | 超时降级机制 | P0 | 2天 | PDL | 实时查询超时降级到缓存 |
| T3.8 | PDL性能优化 | P1 | 3天 | PDL | 关键路径<500μs |
| T3.9 | PDL单元测试 | P1 | 3天 | Tests | 测试覆盖率>85% |

**里程碑**：PDL层功能完整，支持所有外设通信协议

---

#### 阶段4：APP层核心进程实现（6周）

**目标**：实现5个核心业务进程，建立完整的应用架构

**任务清单**

| 任务ID | 任务描述 | 优先级 | 工作量 | 负责模块 | 验收标准 |
|--------|---------|--------|--------|----------|----------|
| T4.1 | 共享内存布局设计 | P0 | 2天 | APP | 定义遥测缓存/心跳表/日志缓冲区 |
| T4.2 | Supervisor进程实现 | P0 | 4天 | APP | 进程监控、心跳检测、自动重启 |
| T4.3 | Telecommand进程实现 | P0 | 5天 | APP | CAN接收、命令解析、2ms应答 |
| T4.4 | Telemetry进程实现 | P0 | 5天 | APP | 遥测采集、数据打包、周期上报 |
| T4.5 | Firmware进程实现 | P1 | 4天 | APP | 固件升级、A/B分区管理 |
| T4.6 | Logger进程实现 | P1 | 3天 | APP | 日志收集、崩溃分析、日志轮转 |
| T4.7 | 实时调度配置 | P0 | 2天 | APP | SCHED_FIFO、优先级、CPU绑定 |
| T4.8 | 进程间通信测试 | P0 | 3天 | Tests | 共享内存、心跳、日志环形缓冲区 |
| T4.9 | 集成测试 | P0 | 3天 | Tests | 5进程协同工作测试 |

**里程碑**：APP层完成，5个进程正常运行并协同工作

---

#### 阶段5：硬实时优化（3周）

**目标**：优化关键路径，确保2ms硬实时响应

**任务清单**

| 任务ID | 任务描述 | 优先级 | 工作量 | 负责模块 | 验收标准 |
|--------|---------|--------|--------|----------|----------|
| T5.1 | 实时性能基准测试 | P0 | 2天 | Tests | 建立性能基线 |
| T5.2 | CAN接收路径优化 | P0 | 3天 | APP/PDL | 中断到应答<1.5ms |
| T5.3 | 内存预分配优化 | P0 | 2天 | APP/PDL | 消除运行时malloc |
| T5.4 | 缓存型遥测优化 | P0 | 2天 | APP | 共享内存访问<10μs |
| T5.5 | 实时型遥测优化 | P0 | 3天 | APP/PDL | 查询+应答<800μs |
| T5.6 | CPU隔离配置 | P1 | 1天 | System | isolcpus内核参数 |
| T5.7 | 中断亲和性配置 | P1 | 1天 | System | CAN中断绑定到实时CPU |
| T5.8 | 实时性能压力测试 | P0 | 3天 | Tests | 1000次测试，99.9%<2ms |
| T5.9 | 性能监控工具 | P1 | 2天 | Tools | 实时延迟统计工具 |

**里程碑**：2ms硬实时响应达标，99.9%置信度

---

#### 阶段6：可靠性增强（4周）

**目标**：实现完整的故障检测和恢复机制

**任务清单**

| 任务ID | 任务描述 | 优先级 | 工作量 | 负责模块 | 验收标准 |
|--------|---------|--------|--------|----------|----------|
| T6.1 | 硬件看门狗集成 | P0 | 2天 | APP/HAL | GPIO喂狗，10秒超时 |
| T6.2 | 进程崩溃检测 | P0 | 2天 | APP/Supervisor | 检测子进程异常退出 |
| T6.3 | 进程自动重启 | P0 | 2天 | APP/Supervisor | 5次/300秒限制 |
| T6.4 | 心跳超时检测 | P0 | 2天 | APP/Supervisor | 5秒超时触发重启 |
| T6.5 | 降级运行模式 | P1 | 3天 | APP | 关键进程崩溃时降级 |
| T6.6 | 故障日志持久化 | P1 | 2天 | APP/Logger | 崩溃日志写入Flash |
| T6.7 | 固件A/B分区管理 | P0 | 4天 | APP/Firmware | 升级失败自动回滚 |
| T6.8 | 固件校验恢复 | P0 | 3天 | APP/Firmware | CRC/SHA256校验 |
| T6.9 | 故障注入测试 | P0 | 4天 | Tests | 模拟各种故障场景 |

**里程碑**：系统可靠性达到航天级标准

---

#### 阶段7：多核异构支持（预研，6周）

**目标**：为未来R5F+A53多核异构架构做技术储备

**任务清单**

| 任务ID | 任务描述 | 优先级 | 工作量 | 负责模块 | 验收标准 |
|--------|---------|--------|--------|----------|----------|
| T7.1 | R5F开发环境搭建 | P2 | 3天 | System | TI CCS + FreeRTOS |
| T7.2 | OSAL FreeRTOS移植 | P2 | 5天 | OSAL | 支持FreeRTOS平台 |
| T7.3 | HAL R5F驱动适配 | P2 | 5天 | HAL | CAN/GPIO/Watchdog |
| T7.4 | RPMsg通信实现 | P2 | 4天 | OSAL | A53<->R5F消息传递 |
| T7.5 | 共享内存跨核访问 | P2 | 3天 | OSAL | A53/R5F共享DDR |
| T7.6 | RemoteProc集成 | P2 | 3天 | System | A53控制R5F启停 |
| T7.7 | R5F实时任务实现 | P2 | 5天 | APP | CAN中断、遥控应答 |
| T7.8 | 核间通信测试 | P2 | 3天 | Tests | 延迟<100μs |

**里程碑**：多核异构架构原型验证通过

---

### 2.3 工作量评估

#### 2.3.1 总体工作量

| 阶段 | 工作量（人天） | 工期（周） | 并行度 |
|------|---------------|-----------|--------|
| 阶段1：基础设施完善 | 20 | 4 | 1人 |
| 阶段2：ACL层实现 | 15 | 3 | 1人 |
| 阶段3：PDL层功能补全 | 25 | 5 | 1-2人 |
| 阶段4：APP层核心进程 | 30 | 6 | 2人 |
| 阶段5：硬实时优化 | 15 | 3 | 1人 |
| 阶段6：可靠性增强 | 20 | 4 | 1-2人 |
| 阶段7：多核异构支持 | 30 | 6 | 1人（预研） |
| **总计** | **155人天** | **31周** | **1-2人** |

#### 2.3.2 关键路径

```
阶段1（基础设施） → 阶段2（ACL层） → 阶段3（PDL层） → 阶段4（APP层） → 阶段5（实时优化） → 阶段6（可靠性）
                                                                                                    ↓
                                                                                            阶段7（多核异构，可并行）
```

**最短工期**：25周（不含阶段7）
**推荐工期**：31周（含阶段7预研）

---

### 2.4 风险评估与应对

#### 2.4.1 技术风险

| 风险项 | 风险等级 | 影响 | 应对措施 |
|--------|---------|------|----------|
| 2ms硬实时无法达标 | 高 | 核心需求不满足 | 1. 提前进行性能基准测试<br>2. 预留优化时间<br>3. 考虑多核异构方案 |
| Redfish/IPMI协议复杂 | 中 | 开发周期延长 | 1. 优先实现核心命令集<br>2. 参考开源实现<br>3. 分阶段交付 |
| 多核异构调试困难 | 高 | 预研周期延长 | 1. 充分利用TI官方工具<br>2. 建立完善的日志机制<br>3. 分步验证 |
| 共享内存同步问题 | 中 | 数据一致性风险 | 1. 使用原子操作<br>2. 无锁数据结构<br>3. 充分测试 |

#### 2.4.2 进度风险

| 风险项 | 风险等级 | 影响 | 应对措施 |
|--------|---------|------|----------|
| 人力资源不足 | 中 | 工期延长 | 1. 优先级排序，聚焦核心功能<br>2. 阶段7可延后<br>3. 考虑外部支持 |
| 需求变更 | 中 | 返工 | 1. 需求冻结机制<br>2. 变更评审流程<br>3. 预留缓冲时间 |
| 测试不充分 | 高 | 质量风险 | 1. 每阶段必须通过测试<br>2. 自动化测试<br>3. 故障注入测试 |

---

### 2.5 质量保证措施

#### 2.5.1 代码质量

- **编码规范**：严格遵守CLAUDE.md中的编码规范
- **代码审查**：所有代码必须经过审查
- **静态分析**：使用cppcheck、clang-tidy
- **单元测试**：测试覆盖率>80%
- **集成测试**：每阶段完成后进行集成测试

#### 2.5.2 性能质量

- **性能基准**：建立性能基线，持续监控
- **压力测试**：1000次测试，统计延迟分布
- **长时间运行测试**：72小时稳定性测试
- **资源监控**：CPU、内存、网络使用率

#### 2.5.3 可靠性质量

- **故障注入测试**：模拟进程崩溃、网络中断、硬件故障
- **恢复时间测试**：验证故障恢复时间<5秒
- **看门狗测试**：验证系统级恢复机制
- **日志完整性**：确保所有故障都有日志记录

---

### 2.6 交付物清单

#### 2.6.1 代码交付物

- [ ] ACL层完整实现（include + src + config）
- [ ] APP层5个进程（Supervisor/Telecommand/Telemetry/Firmware/Logger）
- [ ] OSAL层增强API（实时调度/CPU亲和性/共享内存）
- [ ] PDL层协议实现（Redfish/IPMI）
- [ ] HAL层GPIO驱动
- [ ] 单元测试（覆盖率>80%）
- [ ] 集成测试套件
- [ ] 性能测试工具

#### 2.6.2 文档交付物

- [ ] ACL层设计文档更新
- [ ] APP层设计文档更新
- [ ] API参考手册
- [ ] 部署指南
- [ ] 性能测试报告
- [ ] 可靠性测试报告

#### 2.6.3 配置交付物

- [ ] PMC产品ACL配置文件
- [ ] PCL平台配置文件
- [ ] 系统启动脚本
- [ ] 实时调度配置
- [ ] 看门狗配置

---

### 2.7 里程碑时间表

| 里程碑 | 完成时间 | 关键交付物 | 验收标准 |
|--------|---------|-----------|----------|
| M1：基础设施完善 | 第4周 | OSAL增强API | 单元测试通过 |
| M2：ACL层完成 | 第7周 | ACL层代码+配置 | 业务代码解耦 |
| M3：PDL层完整 | 第12周 | Redfish/IPMI实现 | 协议测试通过 |
| M4：APP层完成 | 第18周 | 5个进程实现 | 集成测试通过 |
| M5：实时性达标 | 第21周 | 性能优化 | 2ms应答99.9% |
| M6：可靠性达标 | 第25周 | 故障恢复机制 | 故障注入测试通过 |
| M7：多核异构原型 | 第31周 | R5F+A53原型 | 核间通信验证 |

---

### 2.8 成功标准

#### 2.8.1 功能完整性

- ✅ 6层架构完整实现（OSAL/HAL/PCL/PDL/ACL/APP）
- ✅ 5个核心进程正常运行
- ✅ 遥控遥测功能完整
- ✅ 固件升级功能完整
- ✅ 日志收集功能完整

#### 2.8.2 性能指标

- ✅ 2ms硬实时应答（99.9%置信度）
- ✅ 缓存型遥测访问<10μs
- ✅ 实时型遥测查询<800μs
- ✅ 进程间通信延迟<100μs

#### 2.8.3 可靠性指标

- ✅ 进程崩溃自动恢复<5秒
- ✅ 硬件看门狗正常工作
- ✅ 72小时稳定性测试通过
- ✅ 故障注入测试100%通过

#### 2.8.4 代码质量

- ✅ 单元测试覆盖率>80%
- ✅ 静态分析0告警
- ✅ 代码审查100%通过
- ✅ 文档完整性100%

---
